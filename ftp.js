import ftp from "basic-ftp";
import dotenv from "dotenv";

dotenv.config();

// Questo file è un placeholder per funzioni FTP future.
// Attualmente non è usato dal server, ma resta nel progetto per espansioni.

export async function uploadImagePlaceholder() {
  return;
}
